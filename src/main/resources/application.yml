spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kakao_community?serverTimezone=UTC&useSSL=false
    username: root
    password: sksdlsrks0718
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발 중에는 create / create-drop / update 중 선택
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug